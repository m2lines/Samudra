# yaml-language-server: $schema=../schemas/DataConfig.json

# We assume you have AWS_ACCESS_KEY_ID and AWS_SECRET_ACCESS_KEY (or other S3 credentials) in your environment.

data_location:
  type: s3
  bucket: oa-fomo
  path: om4_onedeg_compact/OM4.zarr

data_means_location:
  type: s3
  bucket: oa-fomo
  path: om4_onedeg_compact/OM4_means.zarr

data_stds_location:
  type: s3
  bucket: oa-fomo
  path: om4_onedeg_compact/OM4_stds.zarr

static_data_vars: []