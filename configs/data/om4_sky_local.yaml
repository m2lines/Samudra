# yaml-language-server: $schema=../schemas/DataConfig.json

data_location:
  type: local
  path: OM4.zarr

data_means_location:
  type: local
  path: OM4_means.zarr

data_stds_location:
  type: local
  path: OM4_stds.zarr

concurrent_compute: true
num_workers: 4