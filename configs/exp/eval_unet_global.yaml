# @package _global_
defaults:
  - _self_
  - unet: unet

wandb:
  mode: disabled # online, disabled
  project: ocean_emu
  entity: suryadheeshjith
  group: null
  tags: null
  notes: null

### experiment args
experiment: eval
output_dir: ${join_path:${hydra:sweep.dir},${hydra:sweep.subdir}}
data_overlay: ''
grid_path: /scratch/as15415/Data/CM2x_grids/Grid_cm26_Vertices.nc
path_dir: /scratch/sd5313/M2Lines/emulator/Ocean_Emulator/Preds
data_dir: /scratch/sd5313/M2Lines/emulator/Ocean_Emulator/data # /scratch/sd5313/M2Lines/emulator/Ocean_Emulator/.LOCAL/save_data/2024-03-06-save_data_test/data # /scratch/sd5313/M2Lines/emulator/Ocean_Emulator/data
steps: 4

######
use_unet: false
only_unet: false
unet_path: /scratch/as15415/Emulation/Preds
unet_region: "global_21"

ckpt_path: null # changed in command args
network: unet # changed in command args
loss: mse

save_test_data: true # change if already saved in prior experiments
save_clim_data: false # change if already saved in prior experiments
run_gen_pred: true
run_gen_short_pred: true
run_full_pred: true
run_short_pred: true
run_plot_metrics: true
run_plot_animation: true
####

device: "cuda" # keep this cuda to get preds in gpu

# Parameters
exp_num_in: "10"
exp_num_extra: "12"
exp_num_out: "6"
region: "global_21"
N_samples: 4000
N_val: 200
N_test: 3000
interval: 1
hist: 0
lag: 1
lam: 0.05
Nb: 0
factor: 10
lateral: false
