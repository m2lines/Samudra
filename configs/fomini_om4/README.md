# FOMini OM4

These configurations are for FOMini, a single PerceiverIO model that treats
each `(lat, lon)` point as a token and uses learned embeddings of 3D Cartesian
coordinates for both data tokens and output queries.
