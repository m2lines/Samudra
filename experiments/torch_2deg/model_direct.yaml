# SPDX-FileCopyrightText: 2026 Samudra Authors
#
# SPDX-License-Identifier: CC0-1.0

pred_residuals: false
last_kernel_size: 3
pad: circular
checkpointing: null
embedding_dim: 160
perceiver_implementation: auto
# One native latent per 2-degree source cell.
patch_extent: [2.0, 2.0]
processor_residual: true
encoder:
  geometry_mode: sidecar
  native_projection: true
decoder:
  resample_projection: true
  coordinate_resampling: true
  project_before_resample: true
processor:
  ch_width: [160, 480]
  dilation: [1, 1]
  n_layers: [1, 1]
  core_block:
    block_type: conv_next_block
    kernel_size: 3
    activation: capped_gelu
    upscale_factor: 2
    norm: instance
  down_sampling_block: avg_pool
  up_sampling_block: zonally_periodic_upsample
